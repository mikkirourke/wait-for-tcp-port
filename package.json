{
  "name": "wait-for-tcp-port",
  "version": "0.2.0",
  "description": "Nmap based waiting for a list of servers to have opened the specified ports",
  "main": "dist/index.js",
  "scripts": {
    "build": "babel ./src --out-dir dist",
    "test": "mocha --compilers js:babel-core/register ./tests/*.test.js",
    "pretest": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mikkirourke/wait-for-tcp-port.git"
  },
  "author": "mikkirourke",
  "license": "MIT",
  "dependencies": {
    "babel-runtime": "^6.23.0",
    "sleep-promise": "^2.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-core": "^6.24.1",
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-preset-env": "^1.4.0",
    "chai": "^3.5.0",
    "mocha": "^3.4.1"
  }
}
